<Window x:Class="LeafClassification.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeafClassification.Views"
        mc:Ignorable="d"
        Title="Klasifikacija biljaka" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="1" x:Name="LoadLeafRecognizer">Prepoznavanje listova</Button>
        <Button Grid.Column="1" Grid.Row="1" x:Name="LoadAdministration">Administracija</Button>
        <ContentControl x:Name="ActiveItem" Grid.Row="2" Grid.ColumnSpan="3"/>
    </Grid>
</Window>
